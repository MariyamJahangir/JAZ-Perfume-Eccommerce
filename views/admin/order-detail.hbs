<link rel="stylesheet" href="/styles/admin.css">

<section class=" m-0 bgcolor ">

    <div class="row d-flex justify-content-between">

        {{!-- Sidebar --}}
        <div class="aside col-md-2 ">
            <div class="d-flex justify-content-evenly align-items-center">
                <div>
                    <img src="/images/JAZPERFUME.png" alt="" class="logo_image">
                    <p class="fs-6">PERFUMES</p>
                </div>
                <p class="fs-3 fw-2">ADMIN</p>
            </div>

            <div class="nav flex-column px-3">
                <a href="#" class="nav-link ">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/f82b0047e6557aa901c15537d58a7ab6971e1d1d635e2a205c02ec234f11ad61?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Dashboard" loading="lazy">
                    </span>
                    <span class="nav-text">Dashboard</span>
                </a>

                <a href="/admin/products" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/126b232c82239fc3740982b7c8240f84671d1b6e2a4d00141995b3c82d24c4a5?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Products" loading="lazy">
                    </span>
                    <span class="nav-text">Products</span>
                </a>

                <a href="/admin/category" class="nav-link" aria-current="page">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/c46200642d44aab62899b16a25fc8bfe94e9df6fede3e3512b370b3d4f514356?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Categories" loading="lazy">
                    </span>
                    <span class="nav-text">Categories</span>
                </a>

                <a href="/admin/orders" class="nav-link active">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/395e632ba7027a03ce2c35c1aa8bcc4534da249d8c136cdcc01b5043a72ddc87?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Orders" loading="lazy">
                    </span>
                    <span class="nav-text">Orders</span>
                </a>

                <a href="/admin/customers" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/f933064a6bab49b74f8d5722595a056cbddbca1247d9b1a20a7e0fc8d56413a0?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Customers" loading="lazy">
                    </span>
                    <span class="nav-text">Customers</span>
                </a>

                <a href="#" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/ba76937fe489134f0c577be82a18baeadddda8f77194bbcb94734c6db29400c0?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Offers" loading="lazy">
                    </span>
                    <span class="nav-text">Offers</span>
                </a>

                <a href="#" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/cabe95d2f5fab42e35aad96d91245b122dd4d50ddfe8d1d2bceee5ea219442d8?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Coupons" loading="lazy">
                    </span>
                    <span class="nav-text">Coupons</span>
                </a>

                <a href="#" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/2897cc0ce7ace4dd1f7e6d2c1b779540e7fa6d14150e07ac6bd13caecdf99b1a?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Reviews" loading="lazy">
                    </span>
                    <span class="nav-text">Reviews</span>
                </a>

                <a href="#" class="nav-link">
                    <span class="nav-icon">
                        <img src="https://cdn.builder.io/api/v1/image/assets/TEMP/5b4c30ca0a115c712594c2dce59791803bec209977f960ad8b2c1a3d2ecaac23?placeholderIfAbsent=true&apiKey=a98241ab2d584afe817596fb423a2646"
                            alt="Settings" loading="lazy">
                    </span>
                    <span class="nav-text">Settings</span>
                </a>
            </div>
        </div>


        {{!-- Content Section --}}
        <div class="col-md-10">

            {{!-- Breadcrumps --}}
            <div class="row px-2 pt-2">
                <div class="col-8">
                    <nav style="--bs-breadcrumb-divider: '>';" aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="/admin/dashboard"
                                    class="link-dark link-underline-opacity-0">Admin</a>
                            </li>
                            <li class="breadcrumb-item text-dark" aria-current="page"><a href="/admin/orders"
                                    class="link-dark link-underline-opacity-0">Orders</a></li>
                            <li class="breadcrumb-item active text-dark" aria-current="page">Detail</li>
                        </ol>
                    </nav>
                </div>
                <div class="col-4 d-flex justify-content-end pe-5">
                    <button type="submit" class="btn btn-outline-dark btn-sm"
                        onclick="window.location.href='/admin/logout'">Logout<i
                            class="bi bi-box-arrow-right ps-2"></i></button>
                </div>
            </div>

            {{!-- content box --}}
            <div class="row m-0">
                <div class="containerboxcat mt-4 p-4">
                    <div class="row d-flex justify-content-between align-items-center my-4">
                        <div class="col-3">
                            <h3 class="fw-bold">Order Detail</h3>
                        </div>
                    </div>


                    <div class="row ">
                        <div class="col-6 d-flex border p-4">
                            <div class="col-3">
                                <p>Customer :</p>
                                <p>Email :</p>
                                <p>Phone :</p>
                                <p>Address :</p>
                            </div>
                            <div>
                                <p>{{order.userId.firstname}} {{order.userId.lastname}}</p>
                                <p>{{order.userId.email}}</p>
                                <p>{{order.userId.phone}}</p>
                                <p>{{order.shippingAddress.firstname}} {{order.shippingAddress.lastname}} <br>
                                    {{order.shippingAddress.address}} <br> {{order.shippingAddress.locality}},
                                    {{order.shippingAddress.district}}, {{order.shippingAddress.state}} <br> pin:
                                    {{order.shippingAddress.pincode}} <br> {{order.shippingAddress.phone}}</p>
                            </div>
                        </div>
                        <div class="col-6 d-flex border p-4">
                            <div class="col-6">
                                <p>Order ID :</p>
                                <p>Order Date :</p>
                                <p>Payment Method :</p>
                                <p>Total Amount :</p>
                                

                            </div>
                            <div class="col-6">
                                <p>{{order.orderId}}</p>
                                <p>{{formatDate order.orderDate}}</p>
                                <p>{{order.paymentMethod}}</p>
                                <p>Rs. {{order.totalAmount}}</p>
                                

                            </div>

                        </div>

                    </div>


                    <div class="row ">
                        <div class="col-2 d-flex justify-content-center border p-2">
                            <div id="carouselExampleIndicators" class="carousel slide d-flex justify-content-center"
                                data-bs-ride="carousel">
                                <!-- Indicators -->
                                <div class="carousel-indicators">
                                    {{#each order.items}}
                                    
                                    <button type="button" data-bs-target="#carouselExampleIndicators"
                                        data-bs-slide-to="{{@index}}" class="{{#if @first}}active{{/if}}"
                                        aria-current="true" aria-label="Slide {{inc @index}}"></button>
                                    
                                    {{/each}}
                                </div>

                                <!-- Carousel Slides -->
                                <div class="carousel-inner">
                                    {{#each order.items}}
                                    {{#if this.productId.images.length}} <!-- Ensure there are images -->
                                    <div class="carousel-item {{#if @first}}active{{/if}}">
                                        <img src="/uploads/{{this.productId.images.[0]}}" class="d-block w-100" alt="Product Image">
                                    </div>
                                    {{/if}}
                                    {{/each}}
                                </div>

                                <!-- Navigation Controls -->
                                <button class="carousel-control-prev" type="button"
                                    data-bs-target="#carouselExampleIndicators" data-bs-slide="prev">
                                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                                    <span class="visually-hidden">Previous</span>
                                </button>
                                <button class="carousel-control-next" type="button"
                                    data-bs-target="#carouselExampleIndicators" data-bs-slide="next">
                                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                                    <span class="visually-hidden">Next</span>
                                </button>
                            </div>
                        </div>
                        <div class="col-10 border p-0">
                            <table class="table">
                                <thead>
                                    <tr>
                                        <th scope="col">Sl.No</th>
                                        <th scope="col" class="col-3">Products</th>
                                        <th scope="col">Variant</th>
                                        <th scope="col">Quantity</th>
                                        <th scope="col">Price</th>
                                        <th scope="col">Final Price</th>
                                        <th scope="col" class="col-3">Product Status</th>

                                    </tr>
                                </thead>
                                <tbody>
                                    {{#each order.items}}
                                    <tr>
                                        <td scope="col">{{inc @index}}.</td>
                                        <td><a href="/product-details/{{productId._id}}/{{variantId._id}}" class="link-dark link-underline-opacity-0">{{this.productId.name}}</a></td>
                                        <td>{{this.variantId.quantityML}} ML</td>
                                        <td class="text-center">{{this.quantityCount}}</td>
                                        <td>Rs. {{this.price}}</td>
                                        <td>Rs. {{multiply this.quantityCount this.price}}</td>
                                        <td>
                                            {{#if (isEqual this.status "Returned")}}
                                                <div class="d-flex pb-2">
                                                    <!-- Approve Return -->
                                                    <form action="/admin/update-order-status/{{../order._id}}/{{this.productId._id}}" method="POST">
                                                        <input type="hidden" name="status" value="Return Approved">
                                                        <button type="submit" class="btn btn-success me-2">Approve</button>
                                                    </form>

                                                    <!-- Reject Return -->
                                                    <form action="/admin/update-order-status/{{../order._id}}/{{this.productId._id}}" method="POST">
                                                        <input type="hidden" name="status" value="Delivered">
                                                        <button type="submit" class="btn btn-danger">Reject</button>
                                                    </form>
                                                </div>
                                                <p><span class="fw-bold">Return Reason:</span><br>
                                                    {{this.returnReason}}</p>

                                            {{else}}

                                                <div class="d-flex pb-3">
                                                    
                                                    {{#if this.statusOptions.length}}
                                                    <div class="col-7 me-1">
                                                        <form id="setstatus"
                                                            action="/admin/update-order-status/{{../order._id}}/{{this.productId._id}}"
                                                            method="POST">
                                                            <!-- Display the current status as plain text -->

                                                            <select class="form-select" id="status" name="status">
                                                                <!-- Show the current status as a disabled, selected option -->
                                                                <option value="{{this.status}}" selected >
                                                                    {{this.status}}</option>

                                                                <!-- Show only the next available statuses -->
                                                                {{#each this.statusOptions}}                                                                                                                      
                                                                <option value="{{this}}">{{this}}</option>                                                                                                                            
                                                                {{/each}}
                                                            </select>

                                                    </div>
                                                    <div class="col-6">
                                                        <button type="submit" class="btn btn-dark px-2">Set Status</button>
                                                    </div>
                                                        </form>
                                                    {{else}}
                                                    <div class="col">
                                                        <div class="mb-2"><strong>Current Status:</strong> {{this.status}} </div>
                                                        {{#if (isEqual this.status "Return Approved")}} <p><span class="fw-bold">Reason:</span>
                                                    {{this.returnReason}}</p> {{/if}}
                                                        <span class="text-muted">No further status changes allowed</span>
                                                    </div>
                                                    {{/if}}
                                                </div>
                                            {{/if}}
                                        </td>
                                    </tr>
                                    {{/each}}

                                </tbody>
                            </table>

                            

                        </div>
                    </div>


                </div>
            </div>
        </div>
    </div>



</section>



<script>
    document.addEventListener("DOMContentLoaded", function () {
        const statusForm = document.getElementById("setstatus");

        if (statusForm) {
            statusForm.addEventListener("submit", async function (event) {
                event.preventDefault(); // Prevent default form submission

                const formData = new FormData(statusForm);
                const statusValue = formData.get("status");

                // Extract the action URL dynamically from the form
                const actionUrl = statusForm.getAttribute("action");

                try {
                    //const response = await fetch(/admin/update - order - status / ${ order._id } / ${ orderedItem.productId._id }, {
                    const response = await fetch(/admin/update - order - status / ${ order._id } / ${ orderedItem.productId._id }, {
                        method: "POST",
                        headers: {
                            "Content-Type": "application/json",
                        },
                        body: JSON.stringify({ status: statusValue }),
                    });

                    const result = await response.json();

                    if (response.ok) {
                        Swal.fire({
                            icon: 'success',
                            title: 'Success',
                            text: 'Status updated successfully!',
                            confirmButtonText: 'OK'
                        });
                        location.reload(); // Refresh to reflect the new status
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Update Failed',
                            text: result.error || 'Failed to update status.',
                            confirmButtonText: 'OK'
                        });
                    }
                } catch (error) {
                    console.error("Error updating status:", error);
                    Swal.fire({
                        icon: 'error',
                        title: 'An error occurred',
                        text: 'Please try again.',
                        confirmButtonText: 'OK'
                    });
                }
            });
        }
    });


</script>